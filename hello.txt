
var a=[1,2,4,5,6]

var filtered=a.filter((item)=> item>2)

console.log(filtered)

for(let i=0;i<a.length;i++){
	console.log(i)
}

funcion for checkingPAssword(str){
	if(str.includes('@'){
		return passowrd is valid}
}

sign in to account github
 create new repo
name repo
click create button
copy link
clone copied link into your local folder via cmd git clone link

cd repo name

modify or add changes to the local folder

git status

git add .

git commit -m "background and header styles changed, js functional codes implemented"

git push












